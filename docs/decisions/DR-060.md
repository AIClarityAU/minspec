---
id: DR-060
status: accepted
date: 2026-07-14
epic: EPIC-003  # SDD Core Methodology
tier: T2
relates_to: [DR-033, DR-008, DR-004, DR-355]
tags: [dispatch, drain, pr-remediation, hitl, autonomy, egress, security]
title: The drain auto-remediates fixable open-PR problems (ai-review:changes, failing checks, behind-base) — but never merges and never touches human PRs or conflicts
triggered_by: "AIClarityAU/minspec#750 — wire a PR-remediation publish path; founder ask: the drain should look at and fix PRs with problems"
---

# DR-060: Autonomous PR remediation in the drain (fix, re-push, re-review — never merge)

## Context

The continuous drain (#239) triages the inbox and dispatches `agent-ready` issues,
each producing a PR via `dispatch-issue.sh`. Nothing then acted on PRs that came
back with a **problem**: an `ai-review:changes` verdict, a red CI check, or a branch
gone stale behind `main`. Those PRs sat until a human hand-fixed each — the exact
backlog the drain exists to prevent, one layer up.

The founder's ask: *the drain should look at and fix PRs with problems like
conflicts, `ai-review:changes`, failed checks.* This is a new **autonomous
capability** — the drain now writes to existing PR branches, not just opens new ones
— so it needs a recording decision (DR-359 ADR filter — parent register, mmo-platform) and an explicit statement of
where the HITL line stays.

## Decision

**The drain sweeps open, non-draft PRs and auto-remediates *fixable* problems in
place, then re-pushes so CI re-reviews. It never merges, never edits a human PR, and
never auto-resolves a conflict.** Implemented as `scripts/remediate-pr.sh`, invoked
per-PR by the drain's third cycle stage.

### 1. What it fixes (and how)

| Problem | Action |
|---|---|
| `ai-review:changes` (label or red `ai-review` check) | credential-free dev agent addresses the reviewer findings → re-push → CI re-reviews |
| Failing CI checks (test/lint/build/validate) | dev agent reproduces locally (RCDD), fixes the root cause → re-push |
| Behind base | mechanical `git merge origin/main` (no agent) → re-push |

### 2. What it deliberately does NOT do

- **Never merges.** Remediation only *pushes*; the merge keystroke stays human
  (never-wrong / HITL). This is orthogonal to — and does not turn on — the
  deny-by-default **auto-merge** gate (DR-033 §6 / SPEC-024), which remains OFF.
  Remediation *feeds* the review gate; it does not bypass it.
- **Never auto-resolves conflicts.** LLM conflict resolution can silently mismerge.
  A `CONFLICTING`/`DIRTY` PR is surfaced (it already carries `needs-human-review`),
  never touched.
- **Never edits a human PR.** Scope-gated to automation branches (`agent/*`,
  `fix/*`, `feat/*`). A hand-crafted contributor PR is out of bounds.
- **Never mutates `ai-review:*` labels.** CI-as-bot owns them (#600); the re-push
  re-triggers the CI reviewer, which applies the verdict under its own provenance.

### 3. Security model — reused, not re-invented

Identical to `dispatch-issue.sh`: the agent is **credential-free** (no gh / push /
remote / network); the parent does every credentialed op; the **pre-publish egress
guard** (#358) fail-closes on any secret/exfil hit before the push. That guard's
orchestration is the single shared `scripts/lib/agent-egress.sh` (extracted in #747),
so both publish channels — issue dispatch and PR remediation — run the identical
scan and cannot drift (#750). Failing-checks remediation feeds the agent only the
failing check *names* and has it reproduce locally; CI log text (an untrusted-output
injection channel) is never piped into the prompt.

### 4. Bounded autonomy

- **Default-on** (`MINSPEC_DRAIN_REMEDIATE_PRS=1`), off-switch is the same env var
  set to `0`. Consistent with the drain being opt-in per machine already (#239).
- **Attempt cap + crash cap — two independent budgets.** Automated remediation of a PR
  is bounded by `MINSPEC_REMEDIATE_MAX_ATTEMPTS` *genuine* attempts and, separately, by
  `MINSPEC_REMEDIATE_MAX_CRASHES` agent *crashes*. Each is counted from its own distinct
  marker comment, so a crash — which produces no fix at all — never consumes
  genuine-attempt quota, and a genuine attempt never consumes crash quota. Keeping the
  two separate needs more than distinct marker *strings*: one comment body can quote
  another budget's marker (an agent summary discussing this script, an ai-review comment
  quoting its diff), which charged one comment to several budgets at once. Isolation is
  therefore **structural**, enforced in `scripts/remediate-pr.sh`: a single emitter
  sanitises the body's HTML-comment delimiters and appends the marker as the body's last
  line, the counter matches only that terminal position, and only comments this
  automation authored are counted. Either cap tripping stops automation, labels the PR
  `needs-human-review` and posts a one-shot notice naming **every** budget that is
  exhausted — not merely the first one found, and never asserting the other is healthy
  when it is not. (A notice that named one cap while claiming the other was fine sent the
  maintainer to raise that cap, only for the next sweep to trip on the other one.) The
  numeric defaults live in `scripts/remediate-pr.sh` — deliberately not restated here, so
  this DR cannot drift out of date. Total work stays finite (attempts + crashes): no
  runaway quota, no infinite loop.
- **`MINSPEC_REMEDIATE_MAX_ATTEMPTS` returns to its original value; #965's raise is
  superseded.** That raise existed only because a crashed agent — which produces no fix —
  spent genuine-attempt quota, so a single transient crash left one real attempt. The
  separate crash budget removes the cause, and the attempt cap goes back to what it was.
  Recorded because reverting a landed production constant reads as an accident otherwise.
- **Operator identity — `MINSPEC_REMEDIATE_AUTHOR_LOGINS`.** "Comments this automation
  authored" is normally the login of the token the sweep holds, resolved at run time.
  This optional CSV names *additional* accepted logins, for a drain run under more than
  one identity (a machine account plus a maintainer's token, say). It only ever **widens a
  resolved identity — it can never stand in for an unresolved one.** Keying the decision
  on the *merged* list instead meant that a failed identity lookup plus an operator string
  naming some account that had not written the comments looked like provable authorship,
  filtered every comment out, and returned counts of zero — so the cap never tripped at
  all. Login comparison is case-insensitive, as GitHub's own logins are; comparing them
  exactly was the same under-count by another route. **Failure direction is deliberate and
  asymmetric.** For the two budget
  counters, an unresolvable identity falls back to counting *any* author — which can only
  over-count, capping sooner, never un-bounding the loop. For the "capped — needs a human"
  notice, over-counting is the *unsafe* direction (a marker quoted by someone else would
  suppress the notice and hand the PR over with a label and no message — the silent gate
  invariant #2 forbids), so an unresolvable identity instead disables deduplication
  entirely: the notice degrades toward being posted **repeatedly**, never toward silence.
  An allowlist that names nobody (blank, or only separators and whitespace) counts as
  unresolvable rather than as a working filter — a filter matching no one would under-count,
  the un-bounding direction.
- **Quota-aware** — a Claude usage-limit signal during remediation pauses the cycle
  with the same backoff as dispatch (#609).
- **Escalation** — a stuck agent escalates once to opus (DR-355 — parent register, mmo-platform), then surfaces to a
  human rather than shipping a partial fix.

## Consequences

**Positive:**
- The reviewer/CI backlog drains without a human touching each PR, while the human
  keeps the only irreversible act (merge). The autonomy is bounded and reversible.
- Realizes the #358 no-drift rationale by giving `agent-egress.sh` its second
  consumer (#750).

**Negative:**
- The drain now writes to PR branches autonomously — a larger blast surface than
  opening new branches. Mitigated by: automation-branch scope, credential-free
  agent + egress guard, attempt cap, and never-merge.
- A remediation agent can push a wrong-but-green fix; the independent CI reviewer is
  the backstop, and the human still merges. Same trust model as issue dispatch.

## Alternatives considered

- **Surface-only (status quo).** Rejected — leaves the backlog the drain exists to
  prevent; the founder ask is explicitly to *fix*, not just flag.
- **Also auto-merge remediated PRs.** Rejected here — merge is the deny-by-default
  gate's job (DR-033 §6 / SPEC-024), which stays OFF and human-gated; this DR does
  not turn it on.
- **Also remediate human PRs / auto-resolve conflicts.** Rejected — human PRs are
  out of scope, and LLM conflict resolution is a mismerge risk; both are surfaced.

## Costly to Refactor

Cheap to reverse — one new script plus a thin third stage in `drain-inbox.sh`, gated
by one env var; undoable in well under a day by setting `MINSPEC_DRAIN_REMEDIATE_PRS=0`
or reverting the stage. No schema, no stored state, no cross-repo contract. Recorded
because it adds an autonomous write path a future maintainer relies on and because it
must state, on the record, where the merge/HITL line stays.

## Follow-ups (tracked)

- [#750](https://github.com/AIClarityAU/minspec/issues/750) — wire the PR-remediation
  egress path to the shared `agent-egress.sh` (realized by this change).
- [#745](https://github.com/AIClarityAU/minspec/issues/745) — `needs-human-review` not
  cleared when a re-review flips to `ai-review:pass` (a remediated PR keeps the stale
  label until then).
