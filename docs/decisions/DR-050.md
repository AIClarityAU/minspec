---
id: DR-050
title: Shelling the user's own authenticated gh/CLI on explicit consent does not violate the zero-network-in-core invariant
status: accepted
date: 2026-07-01
epic: EPIC-006  # Trust, Consent & Supply Chain
relates_to: [DR-004]
tags: [tier-0, network-boundary, gh-cli, consent, invariants]
triggered_by: "#356 (Tier-0 gh-shell options a/b/c) + review of #365 (ruleset-advisor feature — invariants.test.ts child_process allowlist entry pre-commits an interpretation that needs ratifying)"
---

# DR-050: Shelling the user's own authenticated `gh`/CLI on explicit consent does not violate the zero-network-in-core invariant

> **Born `proposed` per DR-029** (acceptance is a separate human act). Ratifies the
> Tier-0 boundary pre-committed by #365 (ruleset-advisor `invariants.test.ts`
> allowlist entry). Relates to [DR-004](DR-004.md) (Tiered Network Consent Model).

## Context

[AGENTS.md invariant #1](../../AGENTS.md) states: **"MinSpec makes zero network calls in
its core path."** [DR-004](DR-004.md) replaced the old binary "no network" rule with a
three-tier model: Tier 0 (fully offline, no `http`/`https`/`fetch`/`net` imports in
`packages/minspec` or `packages/shared`), Tier 1 (local tool delegation — `gh`, `claude`
— no network code in the extension itself), and Tier 2 (MinSpec Pro / ScroogeLLM).
DR-004's Tier-0 note already cited `gh` CLI issue-creation as an existing shell-out; it
did not, however, settle whether **a shell-out that causes the user's CLI to make a
network call** itself touches invariant #1.

Issue #356 catalogued three options for the ruleset-advisor feature:

- **(a) Allowed-in-core** — gh-shell is simply permitted in the core path; no
  further ceremony.
- **(b) Push to Tier-1 / SealBox** — move any gh-dependent action outside core
  into the agent-execute extension.
- **(c) Tier-gated opt-in with audit** — allow in core but require explicit
  per-action consent and a zero-network default fallback.

PR #365 (ruleset-advisor implementation) pre-committed an interpretation by adding
`gh` to the `child_process` allowlist in `invariants.test.ts`. That interpretation needs
to be explicitly ratified — or overturned — before the test becomes precedent.

## Decision

**Adopt a hybrid of options (a) and (c).** Shelling the user's OWN installed and
authenticated CLI (`gh`, in the same category as `git` which MinSpec already shells) does
**not** count as a MinSpec core network call, **provided all three conditions hold**:

1. **MinSpec opens no socket itself.** The extension invokes the user's tool via
   `child_process`; the tool owns its own auth and network transport. No `http`,
   `https`, `fetch`, or `net` import may appear in `packages/minspec` or
   `packages/shared` — the existing Tier-0 lint gate (DR-004) is unchanged.

2. **Explicit per-action consent.** Every action that will cause a network call
   (e.g. the ruleset READ and the CREATE in the ruleset-advisor feature) must sit
   behind an explicit user opt-in in the UI before it fires. Nothing may trigger
   autonomously.

3. **Zero-network default path intact.** The always-available fallback (e.g. a
   docs link, a copy-to-clipboard prompt) makes **no** network call. The consented
   gh-shell path is strictly additive — the feature degrades gracefully to zero
   network if the user declines or `gh` is absent.

### What this does NOT permit (boundary)

- MinSpec making direct HTTP/HTTPS calls from extension code in any tier.
- Bundling network libraries (`node-fetch`, `axios`, `got`, etc.) in
  `packages/minspec` or `packages/shared`.
- Background or autonomous network activity — every network-triggering action
  requires an in-context user gesture.
- **Autonomous or background egress of user artifacts, spec content, or telemetry
  initiated by MinSpec itself.** Those remain Tier-2 surfaces per DR-004
  (ScroogeLLM / Pro pack only). This is distinct from the user consciously pushing
  their *own* content to their *own* remote via their *own* consented `gh` — that
  is the user's action, not MinSpec's: it is explicitly permitted and is precisely
  what the ruleset-create feature and the existing parking-lot feature
  (`packages/minspec/src/lib/parking-lot.ts`, `gh issue create` with user-authored
  title/body) do. The load-bearing distinction is **who initiates the egress and
  whose data goes where**: MinSpec-initiated exfiltration / telemetry is forbidden;
  a user-directed push of the user's own content to the user's own repository is
  permitted under conditions 2 and 3.

## Amendment (2026-07-01)

**Refines condition 2.** The original condition 2 required explicit per-action
consent for *every* network call, treating the ruleset READ and the ruleset
CREATE identically. In practice this over-gated: it forced a consent toast in
front of a **read-only config probe** that egresses nothing, producing an
"are you sure you want me to check?" prompt with no user-facing risk and no
decision to make. This amendment splits condition 2 by the *class* of the
network action.

**The load-bearing distinction — read-only-config-probe vs. mutation/egress:**

- **Read-only config PROBE → autonomous, no prior consent toast.** A `gh api`
  **GET** of the repo's OWN settings (the `.../rulesets` list read in the
  ruleset-advisor feature; likewise any capability probe such as `gh --version`
  / `gh auth status`) reads back the repository's own configuration and egresses
  **no** user artifacts, spec content, or telemetry. It is the same class of
  action as MinSpec shelling `git fetch` — a read of remote state the user
  already owns, initiated to *decide whether there is anything to offer*. Under
  this amendment such a probe MAY run **autonomously** on init (once `gh` is
  ready and the repo resolves) **without** a preceding consent toast. It still
  honours condition 1 (no socket in the extension; delegated to the user's `gh`)
  and condition 3 (a zero-network fallback remains).

- **Mutating / egressing action → consent-gated (unchanged in spirit).** Any
  network action that **WRITES** to the user's repository or otherwise egresses
  data — the ruleset CREATE (`gh api -X POST .../rulesets`), and a fortiori any
  push of user content — remains behind an **explicit, in-context user gesture**.
  In the reworked ruleset-advisor, the create toast IS that consent: the user's
  click on **"Create ruleset"** is the per-action opt-in for the mutation. Nothing
  writes autonomously. The global/one-time-toggle prohibition in *Costly to
  Refactor* below still stands — this amendment does not replace per-action
  consent for mutations with a global setting; it only exempts the read-only
  probe from needing a *prior* toast at all.

**Crisp rule:** read-only-config-probe = autonomous OK; mutation/egress =
consent-gated (and the mutation's own confirm toast is that consent). This keeps
the Tier-0 story honest — the extension still opens no socket, and MinSpec still
*autonomously* egresses nothing — while removing a consent prompt that guarded a
zero-egress read.

**Status unchanged** (`proposed`); this amendment refines the decision's
condition 2 rather than reversing the decision. Materialised in
`packages/minspec/src/lib/ruleset-advisor.ts` (autonomous `isGhReady` +
`hasRequiredChecksRuleset`; consent-gated `createRequiredChecksRuleset`) and
`packages/minspec/src/commands/init.ts` (`offerRulesetAdvisory` — probe runs
without a toast; a create-offer toast is shown only when the probe finds no
qualifying ruleset).

## Amendment (2026-07-16)

**Extends the read-only-config-probe carve-out to one more read-only endpoint.**
Amendment 2026-07-01 authorised the `.../rulesets` GET to run autonomously as a
read-only config probe. The full-producible-check-set work (#557 / #564 /
SPEC-033 FR-3) added a second read of the same class:
`probeReviewerConfigured` in `ruleset-advisor.ts` issues a
`gh api GET repos/{owner}/{repo}/actions/secrets --jq '[.secrets[].name]'` to
learn **whether the reviewer secrets are present**, so the advisory never
requires a Tier-A status check (`ai-review` / `ready-to-merge`) the repo cannot
yet produce — the #559 permanent-pending deadlock. This amendment **explicitly
enumerates that `actions/secrets` NAMES-only read as an autonomous read-only
config probe**, on the SAME basis as the existing `.../rulesets` read.

**Why it qualifies (identical to the rulesets read):**

- **Reads the repo's OWN config, egresses nothing.** GitHub's
  `GET .../actions/secrets` returns only secret **NAMES and metadata — never the
  secret VALUES** (the API has no endpoint that returns a value). MinSpec reads
  back the repository's own configuration to *decide whether there is anything to
  offer*; it sends no user artifacts, spec content, secret values, or telemetry.
  It is the same class as shelling `git fetch`.
- **Honours condition 1 (no socket).** The extension opens no socket; the read is
  delegated to the user's own authenticated `gh`. No `http`/`https`/`fetch`/`net`
  import is added to `packages/minspec` or `packages/shared`; the Tier-0 lint gate
  is unchanged.
- **Honours condition 3 (zero-network fallback).** The always-available docs link
  (`RULESET_DOCS_URL`) still makes no network call; the probe is strictly additive
  and fail-safe (any read failure ⇒ treat the reviewer as *not* configured ⇒ the
  Tier-A checks stay out of the required set).

**The crisp rule from Amendment 2026-07-01 holds unchanged:**
read-only-config-probe = autonomous OK; mutation/egress = consent-gated (and the
mutation's own confirm toast is that consent). The `actions/secrets` NAMES read is
a read-only config probe, so it runs autonomously in the detection path — with **no
prior consent toast** — alongside the rulesets read. The mutations
(`createRequiredChecksRuleset` POST, `updateRulesetRequiredChecks` PUT) remain
consent-gated exactly as before. MinSpec still opens no socket and still
*autonomously* egresses nothing.

**Why this amendment exists (supersedes the interim consent-gate).** An interim
change (commit `aa93c95`) had gated this secret-names probe behind the write
consent — reading it only *after* the user clicked "Create"/"Add checks". The
founder **overrode that** on #796: the probe reads the repo's own config and
egresses nothing, so it belongs in the same autonomous read-only-probe class as
the rulesets read, not behind a consent gate. This amendment records that decision
and restores the autonomous probe. It also enumerates the **full** reviewer-secret
set the probe must check — `CLAUDE_CODE_OAUTH_TOKEN`, `MINSPEC_APP_ID`, **and**
`MINSPEC_APP_PRIVATE_KEY` — to match `ai-review.yml`'s own guard, closing a
2-of-3 gap that re-opened the #559 deadlock the probe exists to prevent.

**Status unchanged**; this amendment extends the read-only-config-probe carve-out
to a second endpoint rather than reversing the decision. Materialised in
`packages/minspec/src/lib/ruleset-advisor.ts` (`probeReviewerConfigured` +
`REVIEWER_SECRETS` — autonomous, all three secrets) and
`packages/minspec/src/commands/init.ts` (`resolveWantedChecks` /
`offerRulesetAdvisory` — the secret-names probe runs in the autonomous detection
path with no toast in front of it). Materialises the founder's decision on
[minspec #796].

## Amendment (2026-08-25) - PROPOSED, not accepted

**Crosses the carve-out from the repo's config to the user's account, for one
endpoint, in one branch.** Amendments 2026-07-01 and 2026-07-16 both extended the
autonomous read-only-config-probe class to another GET of *the repository's own
settings* (`.../rulesets`, then `.../actions/secrets` NAMES). This amendment is
**different in kind** and is flagged as such: it authorises a read whose subject is
the **user's account**, not the repo. That is a genuine widening of the boundary, not
a further enumeration inside it, so it is recorded as `proposed` and held until the
founder ratifies it under their own account. Implementation of [minspec #1694] must
not begin until then. Triggered by [minspec #1694].

**What it authorises.** A single `gh api GET /user/memberships/orgs` read, issued
**only** in the init advisory's no-GitHub-remote branch
(`offerRulesetAdvisory`, [init.ts:967](../../packages/minspec/src/commands/init.ts#L967)),
to populate the owner picker on the "create a GitHub repo?" offer - so the offer can
name the destination up front instead of asking after the user has committed to the
flow.

**Why `/user/memberships/orgs` and not `/user/orgs`.** Both return the same set of
orgs on the same `read:org` scope. `memberships` additionally returns `role`
(`admin` / `member`) and `state`. Since `role: admin` is where the user could
actually install an App, `memberships` is strictly more signal for an identical
read - preferring the weaker endpoint would buy no privacy and lose the ordering.

**What this amendment explicitly does NOT authorise.** Detecting where the MinSpec
App is installed. That was the originally-requested signal and it is **not reachable
from a Tier-0 extension**; all three candidate endpoints were tested against a real
`gh` OAuth token (scopes `gist`, `read:org`, `repo`, `workflow`) on 2026-08-25:

| Endpoint | Result |
|---|---|
| `GET /user/installations` | 403 - requires a user-to-server token from the App's OAuth flow |
| `GET /orgs/{org}/installation` | 401 - requires an App JWT |
| `GET /repos/{owner}/{repo}/installation` | 401 - requires an App JWT |

MinSpec holds no App private key and runs no OAuth flow, so it can produce neither
credential. `role: admin` is therefore an explicit **proxy for installability**, and
the UI must present it as such. Any copy claiming MinSpec detected an installation
would be false - a never-wrong violation, not a wording preference.

**Why it qualifies as a probe, despite the wider subject:**

- **Egresses nothing.** The read sends no user artifacts, spec content, secret
  values, or telemetry. Nothing reaches AIClarity or any MinSpec-operated endpoint;
  the response is consumed locally to populate a picker and is never persisted.
- **Honours condition 1 (no socket).** Delegated to the user's own authenticated
  `gh`. No `http`/`https`/`fetch`/`net` import is added; the Tier-0 lint gate is
  unchanged.
- **Honours condition 3 (zero-network fallback).** Any failure - `gh` unauthed,
  missing `read:org`, rate limit, non-zero exit - degrades to offering the personal
  account only, plus the existing docs link. The probe is strictly additive and
  fail-safe; it never blocks the offer and never fails the init.
- **Fires under an explicit palette command**, not in the background. The user ran
  `MinSpec: Initialize SDD Structure`.

**Why it is nonetheless bounded tightly.** Org membership can be confidential in an
enterprise, and the `read:org` scope was granted to the GitHub CLI, not to MinSpec.
Zero egress makes the read defensible; it does not make it unremarkable. Two bounds
are therefore load-bearing, not optional hardening:

1. **Branch-scoped.** The probe runs only when `gh` is ready AND no GitHub remote
   resolves - the one branch where the answer changes what is offered. A repo that
   already has a remote never triggers it, which is the overwhelming majority of
   inits.
2. **No persistence.** The org list is not written to settings, cache, or workspace
   state.

**The crisp rule is restated, not replaced.** Read-only probe = autonomous OK;
mutation/egress = consent-gated. The repo creation this probe feeds
(`gh repo create`) is a **mutation** and stays consent-gated exactly as
`createRequiredChecksRuleset` is: the user's click on "Create GitHub repo" is the
per-action consent, and visibility (private default) and owner are chosen inside
that flow. Nothing writes autonomously.

**Status: `proposed`.** Unlike the two preceding amendments, this one is NOT a
status-unchanged refinement - it moves the boundary from the repo's own config to the
user's account, so it is not self-accepting.

> **Ratification is pending, and deliberately so.** The founder indicated acceptance
> in a working session on 2026-08-25, but **no artifact under their own account
> records it**, and [minspec #1694] is the issue that *triggered* this amendment - it
> carries the design discussion, not the ruling, so it must never be cited as proof of
> one. An agent's report of a verbal decision is not ratification of a boundary the
> amendment itself reserved for a human, and a status flip resting on one was blocked
> here by two independent reviewers.
>
> **To ratify:** comment the acceptance from the founder's own GitHub account, then
> change this block to `accepted`, cite that permalink, and delete this note.

The two bounds above (branch-scoped, no persistence) are accepted as
load-bearing conditions of any acceptance, not as optional hardening: an unbounded
org read would NOT be authorised by this amendment even once ratified.

**Follow-ups (tracked):** [minspec #1694] (the repo-create advisory that consumes
this); the owner-picker row is absorbed into SPEC-042's onboarding checklist rather
than shipping as a standalone init toast.

## Amendment (2026-09-22) - PROPOSED, not accepted

**Two asymmetric halves: one more read-only enumeration inside the existing boundary,
and MinSpec's first write to a repository *setting*.** Triggered by [minspec #1809];
specified by [MIN/SP72] (`specs/minspec/SPEC-072-secret-scanning-advisory/requirements.md`),
whose FR-1 holds all implementation until this amendment reads `accepted`. The halves are
separable and may be ratified independently - see *What ratification means*, below.

MinSpec scaffolds a gitleaks secret gate into every repo it initializes
([template-registry.ts:1416](../../packages/minspec/src/lib/template-registry.ts#L1416))
and ships an independent CI witness for it
([template-registry.ts:2143](../../packages/minspec/src/lib/template-registry.ts#L2143),
`.github/workflows/secret-scan.yml`, #1186/#1620). Neither can reach GitHub's own
secret-scanning settings: a workflow cannot configure the repository that runs it, and the
App installation token this repo's bot uses cannot write repo or org settings
([secret-scan.yml:49-57](../../.github/workflows/secret-scan.yml#L49)). The **only** actor
in MinSpec's architecture holding a credential that can is the user's own `gh`, in the
user's own editor - the seam `offerRulesetAdvisory` already occupies. This amendment
decides whether it may.

### Half A - the read: `GET /repos/{owner}/{repo}` (enumeration, not widening)

A single read-only GET of the repository's **own** settings, to learn whether secret
scanning, push protection and non-provider patterns are on, off, or unavailable on this
repo - so the advisory can decide whether there is anything to offer.

Subject-wise this is **identical in kind** to the two reads already authorised: it reads the
repo's own configuration and egresses nothing. It qualifies on the same three grounds as
Amendment 2026-07-16:

- **Egresses nothing.** No user artifacts, spec content, secret values, or telemetry. It
  returns settings *flags* - never alert contents, never a secret. Same class as shelling
  `git fetch`.
- **Honours condition 1 (no socket).** Delegated to the user's own authenticated `gh`. No
  `http`/`https`/`fetch`/`net` import is added to `packages/minspec` or `packages/shared`;
  the Tier-0 lint gate is unchanged.
- **Honours condition 3 (zero-network fallback).** Any failure - unauthed `gh`, missing
  scope, rate limit, unparseable body, absent field - degrades to an all-`unknown` posture,
  which [MIN/SP72] FR-6 requires to be **silent**. The probe is strictly additive and never
  fails the init.

Unlike Amendment 2026-08-25, the subject does **not** move from the repo to the account, so
Half A is an enumeration inside the existing boundary rather than a move of it.

### Half B - the write: `PATCH /repos/{owner}/{repo}` (new in kind)

On the user's explicit click, enable the settings the toast named. Consent-gated exactly as
`createRequiredChecksRuleset` is: the click IS the per-action consent, and nothing writes
autonomously. **The crisp rule is restated, not replaced.**

Half B is nevertheless new in kind and is flagged as such. Every mutation authorised so far
writes a *sub-resource* (`.../rulesets`, `gh issue create`, `gh repo create`). This writes
the **repository object itself**, through an endpoint whose accepted payload is far wider
than the three fields intended. That breadth, not the secret-scanning semantics, is what
needs a human's ruling.

Three bounds are therefore **load-bearing conditions of any acceptance, not optional
hardening** - an unbounded repo-settings write would NOT be authorised by this amendment
even once ratified:

1. **Minimal payload.** The request body carries **only** the secret-scanning settings being
   enabled. It is never assembled by re-serialising a previously-read repository object - a
   retyped payload silently reverting a field the user set elsewhere is a recorded failure
   shape in this repo, not a hypothetical. [MIN/SP72] AC-8 pins the exact key set.
2. **Repo-scoped, enforced as a property.** Every `gh api` path this feature can emit is
   anchored on the resolved `owner/repo`. **No org-, user- or enterprise-scoped path is read
   or written on any branch.** [MIN/SP72] FR-8 requires this as a property over recorded
   argv with an expected-invocation count, not an allowlist of remembered call sites, because
   an allowlist reports clean by omission.
3. **Never asserts an unestablished cause.** A 403 from a token lacking repo-admin and a 403
   from a repo lacking the entitlement are indistinguishable at the call site. Failure copy
   follows the existing honesty discipline (`describeRulesetFailure`,
   [init.ts:820](../../packages/minspec/src/commands/init.ts#L820)): quote GitHub's own
   message, name a plan limit where that is what GitHub said, and otherwise say plainly that
   the request failed.

### What this amendment explicitly does NOT authorise

- **Any org-level configuration, read or write.**
  `secret_scanning_enabled_for_new_repositories` and its siblings are org config that other
  repos never opted into; reaching them from a per-project tool is precisely what
  constitution invariant 3 forbids. Writing them is forbidden **permanently and
  unconditionally** - no future amendment is contemplated. Reading them for context is
  simply **not authorised today**, and would need its own amendment.
- **Reading secret-scanning alerts.** An alert listing returns the neighbourhood of real
  secrets - a different data class from a settings flag. Not covered here; it would need its
  own decision.
- **A global or one-time "allow MinSpec to fix my repos" toggle.** The *Costly to Refactor*
  prohibition below stands unchanged.
- **Bulk or cross-repo operation.** One repo - the open workspace - per advisory pass.
- **Enabling anything silently.** Push protection changes push behaviour for **everyone with
  write access**, not only the clicker; [MIN/SP72] FR-4 requires the toast to disclose that
  before the click, because a consent the clicker did not understand is not consent.

### What ratification means

**Status: `proposed`.** Half A is a status-unchanged enumeration and, taken alone, is the
kind of refinement the two preceding amendments made self-accepting. It is held here anyway
because it arrives attached to Half B, and splitting a ratification the founder has not been
asked for would be the agent choosing the easier half on their behalf.

> **To ratify:** comment the acceptance from the founder's own GitHub account - **naming
> which halves it covers** (A only, or A and B) - then change this block to `accepted`, cite
> that permalink, and delete this note. An agent's report of a verbal decision is not
> ratification; this amendment reserves the ruling for a human, as the 2026-08-25 amendment
> does.
>
> **Ratifying Half A alone is a coherent outcome**, not a half-measure: it ships the advisory
> as read-plus-docs-link (MinSpec tells you, you click through to GitHub), which is strictly
> safer and still closes the "nothing looks" failure that filed [minspec #1809]. Its cost is
> that the fix needs a second, manual act by the user, and governance nags that end in a docs
> link have a poor completion record in this project.

**Follow-ups (tracked):** [minspec #1809] (the advisory itself); [MIN/SP72] (the spec, and
its OQ-1 which puts this amendment's terms to the founder). #1620's still-open half - making
the `Secret scan (gitleaks)` job a *required* status check - is a required-checks change in
the sibling advisory, **not** authorised or addressed here, and [MIN/SP72] OQ-2 decides
whether it is filed separately.

## Rationale

The intent of invariant #1 — and of DR-004's Tier-0 rule — is that MinSpec never
*autonomously* phones home or leaks user data through its core path. A consented,
user-initiated action through the user's own credentialed CLI is the **user's** network
call, not MinSpec's: the extension process opens no socket, holds no auth token, and
sends no data the user has not explicitly triggered. This is exactly the same posture as
shelling `git` — nobody argues `git push` violates the offline invariant because MinSpec
called `git`.

The `gh` CLI is a local, user-owned, user-authenticated binary. Treating it differently
from `git` would require a principled distinction that does not exist at the process or
data-egress level. Both are delegations to a user tool; both honour the "no network code
in the extension itself" boundary.

This reading is consistent with the "visibility-via-UI counts as auditable" principle
(stored in project memory): opt-in + settings text + status-bar indication = compliant
with invariant #8 (auditability). Applying the same logic: consent + graceful-fallback +
zero-socket-in-extension = compliant with invariant #1.

## Consequences

**Positive:**

- Ratifies the `invariants.test.ts` allowlist entry added in #365 (ruleset-advisor),
  unblocking that feature without a tier-boundary change or a SealBox dependency.
- Establishes a durable, reviewable precedent for any future `gh`-shell feature: three
  conditions, all mechanical (no-socket, consent-gate, zero-network-default), easy to
  gate in CI.
- Keeps the Tier-0 air-gapped story honest: the extension process itself still makes
  zero outbound connections; the user's CLI accounts for its own activity.

**Negative:**

- Introduces a second class of Tier-0 action (direct-offline vs. consent-gated
  gh-shell) that a contributor can confuse — placing a gh-shell without the required
  consent gate "in the same bucket" as a plain offline call.
- Each future gh-shell feature carries the same UX tax as Tier-1 features (DR-004
  Consequences): probe CLI availability at activation, degrade gracefully, never hard-error.

## Risks & Mitigations

| Risk | Mechanism / anchor | Mitigation |
|---|---|---|
| Condition 2 (consent) silently dropped | A follow-up PR adds a gh-shell action without a UI opt-in gate, treating this DR's "allowed" as unconditional | The `invariants.test.ts` allowlist test (introduced in #365) must assert not just the allowlisted binary but also that every invocation site in `packages/minspec` is behind a consent-check code path; add a comment anchoring this to DR-050 so the gate is traceable. |
| Condition 3 (fallback) missing | A future feature has no zero-network alternative — the consented path IS the only path | Require a zero-network default path as part of feature spec acceptance for any Tier-0 gh-shell feature; surfaced in the spec gate (DR-012) completeness check. |
| Invariant text not updated | AGENTS.md invariant #1 continues to read "zero network calls in its core path" without qualification, leaving a reader to think gh-shell is banned | Land a companion AGENTS.md update clarifying that consent-gated gh-shell is permitted — the clarification belongs in that file's invariants list, not only in this DR. (See Follow-ups.) |
| DR-004 Tier boundary drift | This DR is read as relaxing Tier-0 more broadly, licensing direct HTTP calls as "user-delegated" | The boundary section above is explicit: no socket, no network library, no autonomous call. The lint gate (no `http`/`https`/`fetch`/`net` imports in `packages/minspec` or `packages/shared`) is unchanged and continues to be the mechanical enforcer. |

## Alternatives Considered

- **Option (b) — push all gh-shell to Tier-1 / SealBox.** Rejected: this forces the
  ruleset-advisor (a lightweight init feature) to carry a full SealBox dependency for
  what is a single, user-consented shell-out. Tier-1 / SealBox is the right home for
  *autonomous* agent dispatch; it is disproportionate for a manually-triggered read.
- **Option (a) alone — unconditional allowance.** Rejected: without the consent gate
  (condition 2) and the zero-network-fallback (condition 3), invariant #1's
  intent — no autonomous network — is not preserved. The hybrid adds those two guards
  without adding a tier.
- **Strict interpretation — any gh-shell violates Tier-0.** Rejected: this would require
  MinSpec to remove its existing `gh`-shell parking-lot feature (noted in DR-004 Context)
  and would draw a distinction between `git` (allowed) and `gh` (banned) that has no
  defensible technical basis. Both delegate to a local, user-owned binary; the
  extension process makes no outbound connection in either case.

## Costly to Refactor

Low. The three conditions are each individually reversible: a future DR could tighten
condition 1 (ban all child_process CLIs from core), relax condition 2, or eliminate
the fallback requirement (condition 3). None of those changes
would require a code rewrite beyond the affected feature's activation path. The
`invariants.test.ts` allowlist is the only structural artefact that materialises this DR
in code; it is a <1-day edit. The precedent this DR sets (gh-shell = user delegation,
not MinSpec egress) is load-bearing only if future contributors rely on it — this record
is where they find it.

> **Note — global/one-time consent toggle NOT licensed here.** Relaxing condition 2
> by replacing per-action consent with a global "allow MinSpec network" setting is
> **not** permitted under this DR — it would re-admit the autonomous-feeling egress
> that per-action consent exists to prevent (a user who toggled a setting once, months
> ago, may not recall they consented to today's action). Any move from per-action to
> global consent requires its own DR to evaluate the trade-off explicitly. Per-action
> consent is the load-bearing guard in this decision, not an implementation detail.

## Follow-ups (tracked)

- **Update AGENTS.md invariant #1** to note that consent-gated gh-shell is explicitly
  permitted under this DR. File a companion issue or inline edit before accepting this DR.
  [minspec #356]
- **Anchor the `invariants.test.ts` allowlist entry** (introduced in #365) with a
  comment referencing DR-050 so the gate stays traceable. Part of #365 PR review.
